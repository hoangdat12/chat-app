export enum MessageType {
  GROUP = 'group',
  CONVERSATION = 'conversation',
}

export enum MessageContentType {
  MESSAGE = 'message',
  IMAGE = 'image',
  NOTIFY = 'notify',
  VIDEO = 'video',
  EMOJI = 'emoji',
}
